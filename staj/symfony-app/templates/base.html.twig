<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    {# Ana başlık bloğunu çeviri anahtarı ile güncelliyoruz #}
    <title>{% block title %}{{ 'app_title'|trans }}{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {# Açıklama, anahtar kelimeler ve yazar meta etiketleri de çevirilebilir olmalı. #}
    {# Şimdilik statik kalabilir, ancak ilerde bunları da çevirmenizi öneririm. #}
    <meta name="description" content="{{ 'site_description'|trans }}"> {# Yeni çeviri anahtarı #}
    <meta name="keywords" content="{{ 'site_keywords'|trans }}">     {# Yeni çeviri anahtarı #}
    <meta name="author" content="{{ 'site_author'|trans }}">       {# Yeni çeviri anahtarı #}

    {# Open Graph başlığını da çeviri anahtarı ile güncelliyoruz #}
    <meta property="og:title" content="{{ 'app_title'|trans }}">
    <meta property="og:description" content="{{ 'site_og_description'|trans }}"> {# Yeni çeviri anahtarı #}
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ 'http://' ~ app.request.host }}">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="/css/gothic-style.css" rel="stylesheet">

    <link href="https://fonts.googleapis.com/css2?family=Creepster&family=Nosifer&family=Butcherman&family=Eater&display=swap" rel="stylesheet">
</head>
<body class="gothic-body">
{% include 'partials/header.html.twig' %}

<main>
    {% block content %}{% endblock %}
</main>

{% include 'partials/footer.html.twig' %}

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/js/gothic-effects.js"></script>
</body>
</html>
