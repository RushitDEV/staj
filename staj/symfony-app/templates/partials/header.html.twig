<header class="gothic-header sticky-top">
    <div class="container">
        <nav class="navbar navbar-expand-lg">
            <div class="navbar-brand d-flex align-items-center">
                <i class="fas fa-skull gothic-skull-icon me-3"></i>
                <h1 class="gothic-title mb-0">Gothizen</h1>
            </div>

            <button class="navbar-toggler gothic-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <i class="fas fa-bars"></i>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link gothic-nav-link" href="#products">
                            <i class="fas fa-fire me-1"></i>Karanlık Koleksiyon
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link gothic-nav-link" href="#about">
                            <i class="fas fa-moon me-1"></i>Hikayemiz
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link gothic-nav-link" href="#reviews">
                            <i class="fas fa-eye me-1"></i>Ruhlar Konuşuyor
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link gothic-nav-link" href="#contact">
                            <i class="fas fa-flame me-1"></i>Karanlık Portal
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="btn gothic-btn-primary ms-2" href="https://www.etsy.com/shop/Gothizen?ref=dashboard-header" target="_blank">
                            <i class="fas fa-external-link-alt me-1"></i>Etsy Mağazası
                        </a>
                    </li>

                    {# Kullanıcı oturum durumu kontrolü #}
                    {% if not app.user %}
                        <li class="nav-item">
                            <a class="nav-link btn btn-auth" href="{{ path('app_login') }}">Giriş Yap</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link btn btn-auth" href="{{ path('app_register') }}">Kayıt Ol</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link btn btn-logout" href="{{ path('app_logout') }}">Çıkış Yap ({{ app.user.username|default(app.user.userIdentifier) }})</a>
                        </li>
                    {% endif %}



                </ul>
            </div>
        </nav>
    </div>
</header>
